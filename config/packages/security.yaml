security:
    # https://github.com/hwi/HWIOAuthBundle/blob/master/docs/3-configuring_the_security_layer.md
    enable_authenticator_manager: true
    # https://symfony.com/doc/current/security.html#loading-the-user-the-user-provider
    providers:
        oauth_provider:
            id: App\Security\OAuthUserProvider
    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        main:
            pattern: ^/
            oauth:
                resource_owners:
                    keycloak: "/admin/login/check-keycloak"
                login_path:   /admin/login
                use_forward:  false
                failure_path: /admin/login
                default_target_path: /admin

                oauth_user_provider:
                    service: App\Security\OAuthUserProvider
                provider: oauth_provider

            logout:
                path: /admin/logout
                target: /admin/login

    # Easy way to control access for large sections of your site
    # Note: Only the *first* access control that matches will be used
    access_control:
        - { path: ^/admin/login, roles: PUBLIC_ACCESS }
        - { path: ^/admin/connect, roles: PUBLIC_ACCESS }
        - { path: ^/admin, roles: ROLE_ADMIN }
        - { path: ^/, roles: ROLE_USER }
