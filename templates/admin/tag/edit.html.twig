{% extends 'admin/base.html.twig' %}

{% block title %}Tag "{{tag.name}}" {{ title_base }}{% endblock %}
{% block page_title %}Modifier le tag{% endblock %}
{% block page_pretitle %}Articles{% endblock %}
{% block page_actions %}
    <a href="{{ path('admin_tags') }}" class="btn btn-primary">Retourner à la liste</a>
{% endblock %}

{% block page_content %}
<div class="container mb-2">
    {{ form_start(form) }}

    {{ form_widget(form) }}

    <button type="submit" class="btn btn-primary">Enregistrer les modifications</button>

    {{ form_end(form) }}
</div>
{% endblock %}
